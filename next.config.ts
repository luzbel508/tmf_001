import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  // Settings for GitHub Pages project site at /tmf_001
  basePath: "/tmf_001",
  assetPrefix: "/tmf_001/",
};

export default nextConfig;
